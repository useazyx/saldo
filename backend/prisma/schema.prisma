// schema.prisma - O mapa do banco do Saldo
// Os models entram na modelagem.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
